fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-dotenv==1.0.1
# 2.x drops the pkg_resources import that 1.4.x still carries, which breaks on
# Python 3.12 installs without setuptools.
razorpay==2.0.1
httpx==0.28.1

# Ledger backend for production (Postgres, via db.py's dialect shim). Always
# installed and imported — db.py imports psycopg unconditionally — but only
# ever *connects* when a postgres:// DSN is actually used; local dev and
# tests run on plain SQLite regardless.
# [binary] avoids a C-compile step in constrained build environments (Vercel,
# CI) at the cost of a slightly larger wheel.
psycopg[binary]==3.2.13
psycopg-pool==3.2.8

# Ed25519 signing and verification for agent payment proofs. The facilitator
# only ever *verifies* here — it holds public keys and no private ones — but
# the same library covers both sides, and the test suite signs with it.
cryptography==46.0.3
